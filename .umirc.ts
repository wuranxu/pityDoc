import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Pity使用文档',
  mode: 'site',
  base: '/pityDoc',
  publicPath: '/pityDoc/'
  // more config: https://d.umijs.org/config
});
